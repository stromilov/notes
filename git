git



/etc/gitconfig         --содержит значения, общие для всех пользователей системы и для всех их репозиториев. 
                         Если при запуске git config указать параметр --system, то параметры будут читаться 
                         и сохраняться именно в этот файл.
                   
~/.gitconfig           --хранит настройки конкретного пользователя. Этот файл используется при указании параметра --global
~/.config/git/config


.git/config            --в том репозитории, который вы используете в данный момент, хранит настройки конкретного репозитория.


git config --global user.name "Name Surname"   --указать данные пользователя
git config --global user.email "e-mail"       
git config --global core.editor emacs          --выбор редактора. по умолчанию vim
git config --list                              --проверка настроек
git config <key>                               --проверить значение конкретного ключа


git add                  --добавить файл в слежение
git commit               --зафиксировать изменения в файле
git push origin master   --отправить изменения на синхронизацию

git clone <адрес_репозитория> <имя_папки>      --клонировать удаленный репозиторий на локальный хост (clone remote repository on local host)

git status               --состояние локального репозитория
            -s           --вывести в краткой форме
            
