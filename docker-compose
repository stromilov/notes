docker-compose



docker-compose up [опции]       --запуск всех контейнеров, определенных в Compose-файле
                  -d            --в фоновом режиме

docker-compose build            --пересоздание всех образов, созданных из файла Dockerfile

docker-compose ps               --вывод информации о состоянии контейнеров, управляемых Compose

docker-compose run              --одноразовый запуск контенера с выполнением одной команды

docker-compose logs             --вывод журнальных записей с цветной подсветкой

docker-compose stop             --остановка контейнеров без их удаления

docker-compose rm               --удаление остановленных контейенров
